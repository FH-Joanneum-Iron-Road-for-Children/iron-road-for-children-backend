quarkus:
  resteasy:
    path: /api

  datasource:
    db-kind: postgresql

  liquibase:
    migrate-at-start: true

#  TODO: oidc

  container-image:
    builder: jib
    name: irfc-backend
    group: FH-Joanneum-Iron-Road-for-Children
    build: true
    push: true
    registry: ghcr.io
    username: PhilipMoser

#  kubernetes-config:
#    enabled: false
#    namespace: ${KUBERNETES_NAMESPACE:default}
#
#  kubernetes-client:
#    namespace: ${KUBERNETES_NAMESPACE:default}

"%prod":
  quarkus:
#    container-image:
#      build: true
#      push: true
    liquibase:
      contexts: prod

"%dev":
  quarkus:
    datasource:
      username: test
      password: test
      devservices:
        port: 5432

    liquibase:
      contexts: dev
      migrate-at-start: true

#    container-image:
#      build: false
#      push: false
